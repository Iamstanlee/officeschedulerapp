import { StyleSheet } from 'react-native';
import { Colors } from '../../../../assets/BaseSheet';

const styles = StyleSheet.create({
  label: {
    color: Colors.white
  }
});

export default styles;
